<% provide(:title, "index category") %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <% @category.each do |c| %>
      <h1><%= c.name %></h1>
      <%= link_to "start", new_lesson_path(:category_id => c.id) %>
    <% end %>
  </div>
</div>

